# A (no attribution footer)

This fixture file deliberately ends without the project-architect attribution
footer so check_07_attribution_footer can flag it as a missing-footer INFO
finding. The last three lines below are pure prose, not the canonical footer.

End of document; no footer follows.
