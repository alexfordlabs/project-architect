# Project Overview — halfdoc

A small CLI demo. PROJECT_REQUIREMENTS.md was never generated in this fixture —
that omission is exactly what B27 (required_docs_generated) must catch at the
Phase-4 exit gate.
