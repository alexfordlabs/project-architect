# {{project_name}} — Overview

This fixture file deliberately contains an unfilled `{{project_name}}`
placeholder so check_08_no_placeholders can flag it as a BLOCKER finding.
A real authored doc would have had the template-substitution step replace
this literal with the project's actual name.

The placeholder above on line 1 is what the check matches.
